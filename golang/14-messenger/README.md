# Kata 14: The Messenger

## Business Scenario

This kata is about a notification sender. The implementation works and the tests describe the current behavior, but the code is intentionally awkward so it can be improved through refactoring.

## Component Usage

The component validates, normalizes, audits, and sends a notification payload through thin forwarding service layers. Treat the tests as the contract while you improve the internal design.

## How to Run

```bash
make test
```
